---
/* The cart on buy buttons. Its own component so the three buy buttons stay
 * one decision — and because this spot has changed art twice already. */
import Icon from './Icon.astro';
---

<Icon name="ui-buy-cart" size="1.45em" />
